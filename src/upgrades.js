/**
 * @file Upgrade scripts for legacy configurations.
 *
 * This module has no public release predating the Atlona rename, so there are
 * no real-world configurations to migrate. Add scripts here when introducing
 * action/feedback/option changes that would break existing user setups.
 */

export const UpgradeScripts = []
